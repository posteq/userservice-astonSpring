spring:
  datasource:
    username: test
    password: test
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master-test.yaml
  jpa:
    properties.hibernate:
      format_sql: true
    show-sql: true
